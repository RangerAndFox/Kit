import { UnavailableSurface } from '@/components/unavailable-surface'

export default function ProjectDetailPage() {
  return <UnavailableSurface title="Project detail is temporarily unavailable" detail="Use the authenticated Control Center and the project’s Google Sheet and Slack Canvases for current information while this page is connected to authoritative project data." />
}
