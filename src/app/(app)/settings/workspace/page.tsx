import { UnavailableSurface } from '@/components/unavailable-surface'

export default function WorkspaceSettingsPage() {
  return <UnavailableSurface title="Workspace settings are read-only" detail="Workspace changes are not yet connected to Kit’s authoritative configuration. Contact a Kit administrator for a verified configuration change." />
}
