# Dormant managed-agent integration

This subsystem is quarantined, not a live Kit runtime. The HTTP registration and toolkit dispatch routes return structural 404s, and webhook-router has no live importer. Definitions in the top-level agents directory are retained as historical reference, not enabled functionality or part of the live model map.

Before reactivation: identify an authenticated consumer, bind workspace and actor to signed identity, test the provider contract and a complete invocation, and remove this notice only once those gates pass. Production Bolt specialists live separately in src/lib/inngest/agents and remain active.
